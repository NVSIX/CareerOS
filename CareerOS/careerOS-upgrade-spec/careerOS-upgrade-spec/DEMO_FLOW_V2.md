# DEMO_FLOW_V2 — Updated Judge Click Path

REPLACES DEMO_FLOW.md as the protected path. Same rule: when build trade-offs
arise, protect this path first. ~4 minutes end to end.

STEP 1 — `/` Landing
Sees the thesis, the warm tactile system, Stage 2 strip.
Action: clicks "Create your profile".

STEP 2 — `/register`
Selects the Candidate tile (feels it press in), enters nothing or anything,
clicks "Get started".
Aha: even registration is considered. Core feature #1 visibly present.

STEP 3 — `/onboarding`
Runs the diagnostic as before (upload → probes → completion).
Action: "View your living portfolio".

STEP 4 — `/candidate/profile`
Sees evidence profile + trajectory. Clicks scenario card "Promotion —
Senior Coordinator" → the projection line MORPHS upward; insight text swaps.
Clicks "Career break — 6 months" → the line dips and recovers.
Aha: futures you can try on. (Career Path Navigator + Life Chapter Designer.)
Action: clicks "View skill map".

STEP 5 — `/candidate/skills` ★
Sees the radial tree: navy verified nodes, pulsing red available nodes,
locked branches. Clicks "Logistics analytics" → detail panel: requirements,
cohort 388, 6–9 months. Clicks "Add to career plan" → node marked Planned.
Aha: the screenshot screen. A career as a living map grounded in cohort data.
Action: sidebar → Jobs.

STEP 6 — `/jobs` → `/jobs/job-001`
Opens the Senior Logistics Coordinator role. Reads match analysis (3 of 4),
then clicks the "Pay benchmark" tab → P25/P50/P75 scale, offer bracket,
RM 6,600 anchor, EPF/SOCSO/EIS/PCB rows, RM 5,445 take-home.
Aha: Malaysian-localised fair pay no other team will show. Applies.
Action: RoleSwitcher → Employer view.

STEP 7 — `/employer/dashboard`
Sees pipeline with match + onboarding risk + NEW flight-risk column; silver
medalists; workforce signals.
Aha: pre-hire, post-hire, and retention intelligence in one table.
Action: clicks Kevin Raj's row (3 signals, red).

STEP 8 — `/employer/retention`
Reads the signal timeline, the 67% churn probability, the two actions.
Aha: the platform watching AFTER the hire — the thesis, weaponised.
Action: back to dashboard → clicks Amira's row.

STEP 9 — `/employer/candidate/cand-001`
Sees the shared evidence profile, the gap reasoning, AND the new First 60
days milestone cards.
Action: dashboard → "Connect Salesforce".

STEP 10 — `/employer/salesforce`
Runs the connection → first record → "72 → 61".
End state: claims → evidence → matching → pay fairness → onboarding →
retention → outcome loop. The full 40-year thesis in ten clicks.

## Narrative spine (v2)
1. Claims become evidence (register, onboarding, profile)
2. Evidence becomes navigable futures (scenarios, skill map)
3. Futures become explainable, FAIR matches (match analysis, pay benchmark)
4. Outcomes feed back — onboarding, retention, 72→61 (the engine never stops)

One-line takeaway unchanged: "the platform that keeps learning after the hire."
